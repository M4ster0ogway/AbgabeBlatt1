---
title: "AbgabeBlatt1"
output: html_notebook
---
Berechnung der höchsten mittleren Temperatur in Grad Celsius mit einer Tabellenkalkulation
```{r}
cpi <- read.table("bike_sharing_data_(with_NAs).csv", sep = ",", header = TRUE) 
max_wert <- max(cpi$average_temperature, na.rm = TRUE)
print(max_wert)
```
